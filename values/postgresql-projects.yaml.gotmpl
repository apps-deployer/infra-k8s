auth:
  username: projects-service
  password: {{ requiredEnv "PG_PROJECTS_PASSWORD" | quote }}
  postgresPassword: {{ requiredEnv "PG_PROJECTS_ADMIN_PASSWORD" | quote }}

primary:
  persistence:
    enabled: true
    size: 1Gi
    storageClass: yc-network-hdd

resources:
  requests:
    memory: 256Mi
    cpu: 100m
  limits:
    memory: 512Mi
    cpu: 500m
