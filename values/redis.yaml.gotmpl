auth:
  enabled: true
  password: {{ requiredEnv "REDIS_PASSWORD" | quote }}

master:
  persistence:
    enabled: true
    size: 1Gi
    storageClass: yc-network-hdd

resources:
  requests:
    memory: 128Mi
    cpu: 50m
  limits:
    memory: 256Mi
    cpu: 200m

replica:
  replicaCount: 0
